# Prettier resolves its config by walking up from each file, but resolves this
# ignore file relative to the working directory. The package scripts run inside
# core/ and worker/, so they pass --ignore-path ../.prettierignore explicitly.
# Dropping that flag as redundant makes every entry here silently stop applying.
dist
node_modules
docs
pnpm-lock.yaml
# Regenerated by MikroORM's migration tooling; formatting it only creates churn.
core/src/migrations/.snapshot-*.json
